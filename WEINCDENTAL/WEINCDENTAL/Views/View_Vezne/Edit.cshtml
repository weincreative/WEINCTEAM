@model WEINCDENTAL.Models.View_Vezne

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>View_Vezne</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.t_tc)

        @Html.HiddenFor(model => model.t_adi)

        @Html.HiddenFor(model => model.t_soyadi)

        @Html.HiddenFor(model => model.t_basvuru)

        @Html.HiddenFor(model => model.t_basvurutarihi)

        @Html.HiddenFor(model => model.Doktoradi)

        @Html.HiddenFor(model => model.Hareketid)

        @Html.HiddenFor(model => model.t_hizmetkodu)

        @Html.HiddenFor(model => model.Hizmetadi)

        @Html.HiddenFor(model => model.t_fiyat)

        @Html.HiddenFor(model => model.Hizmettarih)

        @Html.HiddenFor(model => model.t_odemevarmi)

        @Html.HiddenFor(model => model.t_odenen)

        @Html.HiddenFor(model => model.t_kalan)

        @Html.HiddenFor(model => model.Odemetip)

        @Html.HiddenFor(model => model.Odemetarih)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
