
@{
    ViewBag.Title = "Diş Hizmet Girişi";
}
<!-- MAIN CONTENT -->
<div id="content">
    <!-- row -->
    <div class="row">
        <!-- col -->
        <div class="col-xs-12 col-sm-7 col-md-7 col-lg-4">
            <h1 class="page-title txt-color-blueDark">
                <!-- PAGE HEADER -->
                <i class="fa-fw fa fa-bookmark"></i>
                Diş Hizmet Girişi
            </h1>
        </div>
    </div>

    <section id="widget-grid">
        <!-- row -->
        <div class="row">
            @*@Html.Action("HisHareketBasvuru", "HisHareket")*@
            @Html.Action("DisModul", "HisHareket", new { @id = @ViewBag.bid })
            @*@Html.Action("HisHareketHastaDurum", "HisHareket")*@
            @Html.Action("ListHizmet", "hst_hizmet")
            @*@Html.Action("AyarlarHizmet", "Ayarlar")*@
        </div>
        <!-- end row -->
        <!-- row -->
        <div class="row">
            <!-- a blank row to get started -->
            <div class="col-xs-9 col-sm-9 col-md-9 col-lg-9">
                <div id="content">

                    <!-- widget grid -->
                    <section id="widget-grid" class="">

                        <!-- START ROW -->
                        <div class="row">
                            <article class="col-sm-12 col-md-12 col-lg-12">
                                <div id="refreshtable">
                                    @Html.Action("_NewHareket")
                                </div>

                                @Html.Action("_DisHHareket", "View_HizHareket", new { @id = ViewBag.bid })

                            </article>
                            <!-- NEW COL START -->
                            <!-- END COL -->
                            <!-- NEW COL START -->

                        </div>
                        <!-- END ROW -->
                    </section>
                    <!-- end widget grid -->


                </div>


                <!-- your contents here -->
            </div>

        </div>
        <!-- end row -->
    </section>
    <!-- end widget grid -->
</div>

<script src="~/Scripts/jquery/jquery-1.9.1.min.js"></script>
<script type="text/javascript">

    var secilenHizmet;
    var secilenDis;
    var hizmetAd;
    var hizmetFiyat;
    function SeciliHizmet(SHid, SHAd, SHFiyat) {
        secilenHizmet = SHid;
        hizmetAd = SHAd;
        hizmetFiyat = SHFiyat;
    }

    function SeciliDis(SDid) {
        secilenDis = SDid;
        if (secilenHizmet != null) {
            if (secilenDis == null) {
                $.smallBox({
                    title: "İPTAL",
                    content: "<i class='fa fa-clock-o'></i> <i>Seçili Diş getirilemedi...</i>",
                    color: "#C46A69",
                    iconSmall: "fa fa-times fa-2x fadeInRight animated",
                    timeout: 4000
                });
            } else {
                $.smallBox({
                    title: "OKAY",
                    content: "<i class='fa fa-clock-o'></i> <i>Seçili " + "Diş No : " + secilenDis + " Hizmet No : " + secilenHizmet + "</i>",
                    color: "#739E73",
                    iconSmall: "fa fa-thumbs-up bounce animated",
                    timeout: 4000
                });
                Ekle(secilenDis, secilenHizmet, hizmetAd, hizmetFiyat);
            }
        } else {
            $.smallBox({
                title: "İPTAL",
                content: "<i class='fa fa-clock-o'></i> <i>Hizmet Seçmediniz...</i>",
                color: "#cc1818",
                iconSmall: "fa fa-times-circle bounce animated",
                timeout: 4000
            });
        }
    }
    function Ekle(dkod,hkod, had, hfiyat) {

        var row = $('#newHizmet').closest('tr').clone();
        row.find("#HKod").text(hkod);
        row.find("#HAd").text(had);
        row.find("#DKod").text(dkod);
        row.find("#Fiyat").text(hfiyat);
        row.find("#CDurum").text('Tek Diş');
        $('#newHizmet').closest('tr').after(row);
        $('input[type="button"]', row).removeClass('AddNew').addClass('RemoveRow').val('Remove item');
      //  row.find("input").id = "asd";

        var rows = document.getElementById("tblbody").rows[0];
       // alert(rows);
        var Cells = rows.getElementsByTagName("label");
       // alert(Cells);
        var remove = Cells[0].innerText;
       // alert(remove);

        //if (remove == null || remove == '') {
        //    document.getElementById('tblHizmet').deleteRow(1);
        //}

    }
   
    $('table').on('click', '.RemoveRow', function () {
            //Some code

        $(this).closest('tr').remove();
        @*var rows = document.getElementById("tblbody").rows.length;
        if (rows <= 1) {
            $('#refreshtable').load("@Url.Action("_NewHareket")");
        }*@
    

   });

   

</script>

